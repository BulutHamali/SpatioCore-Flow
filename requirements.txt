# Generated from pyproject.toml — use 'uv pip install -r requirements.txt'
# or: uv pip install -e ".[dev]"

# ── Core omics stack ──────────────────────────────────────────────────────────
anndata>=0.10
scanpy>=1.10
squidpy>=1.4

# ── Agent orchestration & LLM routing ────────────────────────────────────────
crewai>=0.55
litellm>=1.40

# ── Schema enforcement ────────────────────────────────────────────────────────
pydantic>=2.7
pydantic-settings>=2.3

# ── API & UI ──────────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.30
streamlit>=1.35

# ── Databases ────────────────────────────────────────────────────────────────
chromadb>=0.5
psycopg2-binary>=2.9
sqlalchemy>=2.0
redis>=5.0

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0
tenacity>=8.3
httpx>=0.27
rich>=13.7

# ── Dev / test (optional) ─────────────────────────────────────────────────────
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
ruff>=0.4
mypy>=1.10
